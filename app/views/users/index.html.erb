<div class="information">
  <h2 class="title">Listing employees</h2>
  <p class="meta"><span class="date">Employees from "Company 1"</span></p>
  <div style="clear: both;">&nbsp;</div>
  <div class="entry">
    <table class="list">
      <tr>
        <th>Login</th>
        <th>Name</th>
        <th>Last name</th>
        <th>Email</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.login %></td>
        <td><%= user.name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
        <td><%= link_to 'Destroy', user, :confirm => 'Are you sure?', :method => :delete %></td>
      </tr>
    <% end %>
    </table>
    
    <p class="links"><%= link_to 'Add another employee', new_user_path %></p>
  </div>
</div>